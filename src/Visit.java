/**
 * Created by user on 10.05.2017.
 */
public interface Visit
{
    //we need this Interface just for defining our visit-methods
    String visit(BinaryTreeNode node);


    //Maybe I wasn't intelligent enough to understand what the methods should actually do
    //but the only thing I found about "pre-visit, in-visit and post-visit" was the
    //arithmetic example...
    //So I'll just let the methods return a string to build up the arithmetical expression
}
